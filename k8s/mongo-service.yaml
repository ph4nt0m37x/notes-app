apiVersion: v1
kind: Service
metadata:
  name: mongodb
  namespace: notes-app
spec:
  selector:
    app: mongodb
  ports:
    - port: 27017
      targetPort: 27017
  clusterIP: None   # important for StatefulSet (headless service)